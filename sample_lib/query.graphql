query Person($id: String!) {
  me(id: $id) {
    # age
    label,
    name,
    # motherName,
    mother {
      name,
      birthDate,
      # id,
      # label,
      # something,
      # birthDate_2,
      # url
    }
    father {
      name,

      # id,
      # label,
      # something,
      birthDate,
      # birthDate_2,
      # url
    },
    ancestors {
      name
    }
  #   id,
  #   something,
    birthDate,
    # ancestorsLabels {
    #   name
    # }
  #   birthDate_2,
  #   url
  }
}
